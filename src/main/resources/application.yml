spring:
  application:
    name: flight-monitor
  profiles:
    active: dev
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092

monitor:
  worker:
    count: 3

notification:
  mode: kafka
